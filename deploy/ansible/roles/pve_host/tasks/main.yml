---
- name: Persistance des paramètres sysctl sur Proxmox
  sysctl:
    name: "{{ item.key }}"
    value: "{{ item.value }}"
    sysctl_set: true
    state: present
    reload: true
  loop:
    - { key: "net.bridge.bridge-nf-call-iptables", value: "1" }
    - { key: "net.bridge.bridge-nf-call-ip6tables", value: "1" }
    - { key: "net.ipv4.ip_forward", value: "1" }

- name: Chargement et persistance du module br_netfilter (si non built-in)
  lineinfile:
    path: /etc/modules-load.d/k3s-pve.conf
    line: br_netfilter
    create: true
  register: pve_modules

- name: Application immédiate du module br_netfilter
  command: modprobe br_netfilter
  when: pve_modules.changed
  ignore_errors: true # Au cas où il serait déjà built-in

# --- Passthrough GPU + Input pour le LXC Media Center ---

- name: Passthrough GPU/Input — droits cgroup2 pour le LXC media
  lineinfile:
    path: /etc/pve/lxc/{{ media_lxc_id }}.conf
    line: "{{ item }}"
    create: false
  loop:
    # AMD GPU (DRM/KMS) — major 226
    - "lxc.cgroup2.devices.allow: c 226:* rwm"
    # Framebuffer /dev/fb0 — major 29
    - "lxc.cgroup2.devices.allow: c 29:0 rwm"
    # Input devices (clavier, manettes, IR) — major 13
    - "lxc.cgroup2.devices.allow: c 13:* rwm"
    # ALSA audio — major 116
    - "lxc.cgroup2.devices.allow: c 116:* rwm"
    # /dev/tty* — major 4
    - "lxc.cgroup2.devices.allow: c 4:* rwm"
    # /dev/tty0 — nécessaire pour GBM/KMS (prise de contrôle display)
    - "lxc.cgroup2.devices.allow: c 4:0 rwm"
    # USB devices — major 189 (manettes USB)
    - "lxc.cgroup2.devices.allow: c 189:* rwm"
    # uinput — major 10, minor 223 (Steam crée des gamepads virtuels pour Proton)
    - "lxc.cgroup2.devices.allow: c 10:223 rwm"
    # Bind mounts
    - "lxc.mount.entry: /dev/dri dev/dri none bind,optional,create=dir"
    - "lxc.mount.entry: /dev/fb0 dev/fb0 none bind,optional,create=file"
    - "lxc.mount.entry: /dev/tty0 dev/tty0 none bind,optional,create=file"
    - "lxc.mount.entry: /dev/input dev/input none bind,optional,create=dir"
    - "lxc.mount.entry: /dev/uinput dev/uinput none bind,optional,create=file"
    # /sys rw — requis pour systemd-udevd (libinput → clavier/souris/manette)
    - "lxc.mount.auto: proc:rw sys:rw"
    # Désactiver seccomp — requis pour Steam (syscalls bloqués → SIGTRAP crash)
    - "lxc.seccomp.profile:"
  when: deploy_media | bool
  register: media_lxc_config

# Unbind fbcon du GPU AMD pour que le LXC puisse prendre le DRM master
- name: Udev rule — renderD128 group=render (persistant)
  copy:
    dest: /etc/udev/rules.d/99-amdgpu-render.rules
    content: |
      KERNEL=="renderD128", GROUP="render", MODE="0660"
  when: deploy_media | bool

- name: Service systemd — unbind fbcon AMD au boot (libère DRM pour Kodi LXC)
  copy:
    dest: /etc/systemd/system/unbind-fbcon.service
    content: |
      [Unit]
      Description=Unbind fbcon from AMD GPU (free DRM master for Kodi LXC)
      After=systemd-udev-settle.service
      Before=pve-guests.service

      [Service]
      Type=oneshot
      RemainAfterExit=yes
      ExecStart=/bin/sh -c 'for i in $(seq 1 10); do [ -w /sys/class/vtconsole/vtcon1/bind ] && break || sleep 1; done; echo 0 > /sys/class/vtconsole/vtcon1/bind'
      ExecStop=/bin/sh -c 'echo 1 > /sys/class/vtconsole/vtcon1/bind'

      [Install]
      WantedBy=multi-user.target
  when: deploy_media | bool

- name: Activer unbind-fbcon au démarrage PVE
  systemd:
    name: unbind-fbcon
    enabled: true
    daemon_reload: true
  when: deploy_media | bool

- name: Ajouter /dev/snd au passthrough LXC media
  lineinfile:
    path: /etc/pve/lxc/{{ media_lxc_id }}.conf
    line: "lxc.mount.entry: /dev/snd dev/snd none bind,optional,create=dir"
    create: false
  when: deploy_media | bool

- name: Supprimer entrées BT HCI incorrectes du LXC media (hci0 n'a pas de nœud device)
  lineinfile:
    path: /etc/pve/lxc/{{ media_lxc_id }}.conf
    line: "{{ item }}"
    state: absent
  loop:
    - "# Bluetooth HCI passthrough (Realtek RTL8852BU)"
    - "lxc.cgroup2.devices.allow: c 248:* rwm"
    - "lxc.mount.entry: /dev/hci0 dev/hci0 none bind,optional,create=file"
  when: deploy_media | bool

# --- Bluetooth sur l'hôte PVE (DS4 BT géré sur l'hôte, events forwarded via /dev/input) ---

- name: Installer bluez sur l'hôte PVE
  apt:
    name: bluez
    state: present
    update_cache: false
  when: deploy_media | bool

- name: Service systemd — binding Realtek RTL8852BU (0bda:b85b) vers btusb au boot
  copy:
    dest: /etc/systemd/system/btusb-realtek.service
    content: |
      [Unit]
      Description=Bind Realtek RTL8852BU USB BT adapter to btusb driver
      After=bluetooth.service
      Wants=bluetooth.service

      [Service]
      Type=oneshot
      RemainAfterExit=yes
      ExecStart=/bin/sh -c 'modprobe btusb && echo "0bda b85b" > /sys/bus/usb/drivers/btusb/new_id || true'

      [Install]
      WantedBy=multi-user.target
  when: deploy_media | bool

- name: Activer btusb-realtek et bluetooth au démarrage PVE
  systemd:
    name: "{{ item }}"
    enabled: true
    state: started
    daemon_reload: true
  loop:
    - bluetooth
    - btusb-realtek
  when: deploy_media | bool

- name: Déployer audio-switch sur l'hôte PVE
  copy:
    src: audio-switch.sh
    dest: /usr/local/bin/audio-switch
    mode: '0755'
  when: deploy_media | bool

- name: Déployer bt-pair sur l'hôte PVE
  copy:
    src: bt-pair.sh
    dest: /usr/local/bin/bt-pair
    mode: '0755'
  when: deploy_media | bool

# --- Stockage secondaire (SSD NVMe — données partagées) ---

- name: Créer le point de montage /mnt/data
  file:
    path: /mnt/data
    state: directory

- name: Monter le SSD données (/dev/nvme0n1p1) via fstab
  mount:
    path: /mnt/data
    src: /dev/nvme0n1p1
    fstype: ext4
    opts: defaults,noatime
    passno: "2"
    state: mounted

- name: Créer l'arborescence de données sur le SSD
  file:
    path: "/mnt/data/{{ item }}"
    state: directory
    owner: "1000"
    group: "1000"
  loop:
    - games/steam
    - games/non-steam
    - movies
    - movies/joal-import
    - paperless
    - paperless/data
    - paperless/media
    - paperless/consume
    - paperless/export
    - nextcloud
    - backup

- name: Droits spéciaux joal-import (777)
  file:
    path: /mnt/data/movies/joal-import
    mode: '0777'

- name: Bind-mount SSD données dans le LXC media
  lineinfile:
    path: /etc/pve/lxc/{{ media_lxc_id }}.conf
    line: "mp0: /mnt/data,mp=/mnt/data"
    create: false
  when: deploy_media | bool
  register: media_games_mount

- name: Bind-mount SSD données dans le LXC K3s
  lineinfile:
    path: /etc/pve/lxc/{{ lxc_id }}.conf
    line: "mp0: /mnt/data,mp=/mnt/data"
    create: false
  when: k3s_target == 'lxc'
  register: k3s_data_mount

- name: Passthrough GPU/TUN + proc:rw pour le LXC K3s
  lineinfile:
    path: /etc/pve/lxc/{{ lxc_id }}.conf
    line: "{{ item }}"
    create: false
  loop:
    - "lxc.cgroup2.devices.allow: c 226:* rwm"
    - "lxc.mount.entry: /dev/dri dev/dri none bind,optional,create=dir"
    - "lxc.cgroup2.devices.allow: c 10:200 rwm"
    - "lxc.mount.entry: /dev/net dev/net none bind,optional,create=dir"
    - "lxc.mount.auto: proc:rw sys:rw"
  when: k3s_target == 'lxc'
  register: k3s_lxc_config

- name: Redémarrage du LXC media (si config passthrough modifiée)
  command: pct reboot {{ media_lxc_id }}
  when:
    - deploy_media | bool
    - media_lxc_config.changed or (media_games_mount is defined and media_games_mount.changed)
  ignore_errors: true

- name: Redémarrage du LXC K3s (si config modifiée)
  command: pct reboot {{ lxc_id }}
  when:
    - k3s_target == 'lxc'
    - (k3s_data_mount is defined and k3s_data_mount.changed) or (k3s_lxc_config is defined and k3s_lxc_config.changed)
  ignore_errors: true
