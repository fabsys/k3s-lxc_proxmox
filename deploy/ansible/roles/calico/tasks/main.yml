---
- name: Ajout du repo Helm Calico (Tigera)
  shell: helm repo add projectcalico https://docs.tigera.io/calico/charts && helm repo update
  changed_when: false
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml

- name: Configuration du pool IP Calico
  copy:
    dest: /etc/rancher/k3s/calico-values.yaml
    owner: root
    group: root
    mode: "0600"
    content: |
      installation:
        calicoNetwork:
          ipPools:
            - name: k3s-pod-network
              cidr: {{ k3s_cluster_cidr }}
              blockSize: 26
              encapsulation: IPIP
              natOutgoing: Enabled
              nodeSelector: all()

- name: Installation de Calico via Tigera Operator
  shell: |
    helm upgrade --install calico projectcalico/tigera-operator \
      --version v{{ calico_version }} \
      --namespace tigera-operator \
      --create-namespace \
      --values /etc/rancher/k3s/calico-values.yaml
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml

- name: Attente que l'opérateur Tigera soit prêt
  shell: |
    kubectl -n tigera-operator rollout status deployment/tigera-operator --timeout=300s
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml

- name: Attente que les nœuds soient Ready (Calico configuré)
  shell: |
    kubectl wait node --all --for=condition=Ready --timeout=300s
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  retries: 10
  delay: 15
  register: result
  until: result.rc == 0
